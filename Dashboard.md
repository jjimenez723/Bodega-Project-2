Act as a Senior Full-Stack Developer. We are building a new profitability and data-tracking dashboard for the Bodega Project, an initiative that manages a decentralized network of community gardens, faith centers, and bodegas to mitigate urban food waste.Our frontend relies on a React-based architecture (Next.js) and we will strictly use Firebase for our backend, specifically leveraging Firestore for real-time NoSQL database management and Firebase Auth for Role-Based Access Control (RBAC).The Objective:Generate the frontend component structure and the Firestore database schema required to track and compare the profitability of growing food in traditional soil versus hydroponic systems.1. Database Schema (Firestore):Please design a scalable NoSQL schema that includes collections for global settings, user roles, and harvest batches. The data points we need to track per batch include:Shared Logistics & Consumables (Applies to Both): Cost of seeds, cost of water, cost of labor, cost of materials, cost of gas for transport, and the estimated externalities of fossil fuel burning.Hydroponic-Specific Variables: Cost of electricity ($e^-$ within the machine).Harvest Metrics (Applies to Both): Total weight yielded, weight missed (unsellable/spoilage), and weight compostable.Calculated Financials: Sellable Yield (Total Weight - Weight Missed), Revenue (Price per lb * Sellable Yield), and Profit (Revenue - Total Costs).2. Role-Based Access Control (RBAC):Outline the Firebase Security Rules and the frontend context needed to support three user tiers:Admin / Lead Engineer: Full CRUD access. Can edit global financial constants (default price per lb, standard labor rates, electricity/gas baselines) in a master settings panel.Project Manager: Can view all profitability analytics, create new harvest batches, and override baseline costs for specific batches if local variables fluctuate.Data Entry / Bodega Partner: Restricted access. Can only input daily/weekly harvest metrics (Total Weight, Weight Missed, Weight Compostable) via a simplified form.3. UI / Component Architecture:Provide the React/Next.js component tree for the dashboard. We need:A tabbed input view separating "Soil" and "Hydroponics" forms. Crucially, transport gas and logistics costs must be present on BOTH tabs, as produce from both methods requires distribution. The Hydroponics tab will dynamically add the electricity cost input.A Global Settings component for Admins to adjust baseline metrics.An Analytics component that fetches the Firestore data, executes the profit formulas, and renders the comparative efficiency of both growing methods.Deliverables Requested:The Firestore schema structure (as JSON or TS interfaces).The Firebase Security Rules to enforce the RBAC tiers.The initial boilerplate code for the main React dashboard and input components.